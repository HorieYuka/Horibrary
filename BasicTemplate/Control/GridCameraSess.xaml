<UserControl x:Class="BasicTemplate.Control.GridCameraSess"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BasicTemplate.Control"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid AllowDrop="True"
          MouseMove="ItemMouseMove">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Row="0"
                        Grid.Column="0"
                        Content="{Binding CameraShow[0]}" />
        <ContentControl Grid.Row="0"
                        Grid.Column="1"
                        Content="{Binding CameraShow[1]}" />
        <ContentControl Grid.Row="1"
                        Grid.Column="0"
                        Content="{Binding CameraShow[2]}" />
        <ContentControl Grid.Row="1"
                        Grid.Column="1"
                        Content="{Binding CameraShow[3]}" />
    </Grid>
</UserControl>
